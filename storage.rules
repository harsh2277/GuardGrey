rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // GuardGrey uploads are only available to signed-in app users.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
